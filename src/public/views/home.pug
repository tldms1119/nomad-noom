doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1 Noom
        main
            div#welcome
                form
                    input#roomName(placeholder="room name", required, type="text")
                    input#nickname(placeholder="nickname", required, type="text")
                    button Enter Room
                h4 Open Rooms :
                ul
            div#room
                h3
                ul
                form#msg
                    input(placeholder="message", required, type="text")
                    button Send
        // 기존에는 브라우저에서 제공하는 WebSocket 을 사용했지만 SocketIO 와는 호환되지 않으므로 back-end 에서 보내주는 script 참고
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app_socketIO.js")


